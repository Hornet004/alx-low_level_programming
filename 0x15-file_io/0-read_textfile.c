#include <stdio.h>
#include <fcntl.h>
#include "main.h"

ssize_t read_textfile(const char *filename, size_t letters)
{
  ssize_t = fd;
  fd = open("filename", O_RDWR);
  if(filename == NULL)
    return(0);
  retun(0);
}
