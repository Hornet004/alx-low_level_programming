#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for ALX School students.
 *
 * Return: Always 0.
*/
char *_strcat(char *dest, char *src)
{
char temp[] = dest + src;
return (temp);
}
